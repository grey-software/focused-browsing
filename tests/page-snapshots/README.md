# Page Snapshots

These files are HTML snapshots of real logged-in pages used for selector contract checks.

Rules:

1. Keep each snapshot focused on DOM shape, not visual fidelity.
2. Update snapshots when selectors are intentionally migrated or pages materially change.
3. Never commit cookies, tokens, or personal account data.
